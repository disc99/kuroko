package {{packageName}};

/**
 * {{bean.className}}
 * 
 * <p>
 * Don't modify this file!
 * This file is automatically generated for annotation processor.
 */
public class {{className}} {
    private final {{bean.type}} bean;

    public {{className}}({{bean.type}} bean) {
        this.bean = bean;
    }

    {{#each methods}}
    public {{returunType}} {{methodName}}() {
        try {
            Field field = {{className}}.class.getDeclaredField({{fieldName}});
            field.setAccessible(true);
            return field.get(bean);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
    }
    {{/each}}

    {{#each methods}}
    public void {{methodName}}({{inputType}} value) {
        try {
            Field field = {{className}}.class.getDeclaredField({{fieldName}});
            field.setAccessible(true);
            field.set(bean, value);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
    }
    {{/each}}

}
